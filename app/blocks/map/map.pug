mixin map()
  +b.map&attributes(attributes)#map
    block

mixin ui-kit-map
  +ui-section
    +ui-section-head
      +ui-title Карта
      +ui-path
        p Блок: blocks/map/map
      +ui-description
        p Сделано через Leaflet.js
        p
          a(href="https://leafletjs.com/" target="_blank") Документация

    +ui-section-content()
      +map()

    //- При +ui-section-content(true) контейнер отсутствует
    //- Для перечисления инлайновых блоков используется миксин +ui-elements-list()
    //- Рамка вокруг особой секции: +ui-special()
